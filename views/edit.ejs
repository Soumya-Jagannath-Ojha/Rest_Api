<% layout("/layouts/boilerplate") %>

<div class="row mt-3">
    <div class="col-8 offset-2">
<body>
    
        <h2>Edit your post</h2>
        <p>User id: <%= user._id %></p>
        <p>User name: @<%= user.name %></p>

        <form method="post" action="/users/<%= user._id %>?_method=PUT">
            Age: <input type="text" placeholder="<%= user.age %>" name="age" /><br><br>
            country: <input type="text" placeholder="<%= user.country %>" name="country" /><br><br>
            <button>Submit</button>
        </form>
    
        
</body>
</div>
</div>
    
    <style>
        .Edit{
            display: block;
            justify-content: center;
            align-items: center;
            height: 500px;
        }
    </style>
